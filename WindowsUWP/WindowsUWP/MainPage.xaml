<Page
    x:Class="WindowsUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsUWP"
    xmlns:model="using:WindowsUWP.models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loading="Page_Loading">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Hub Header="Статистика">
            <HubSection x:Name="everyDaySection" Header="Ежедневная" Width="350">
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">

                            <StackPanel Orientation="Horizontal" Padding="3">
                                <TextBlock Text="C:" VerticalAlignment="Center"/>
                                <DatePicker HorizontalAlignment="Right" Padding="5" VerticalAlignment="Top"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Padding="3">
                                <TextBlock Text="По:" VerticalAlignment="Center"/>
                                <DatePicker HorizontalAlignment="Right" Padding="5" VerticalAlignment="Top"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Padding="3">
                                <TextBlock Text="Сайты: " Padding="5" VerticalAlignment="Center"/>
                                <ComboBox HorizontalAlignment="Left" Padding="5" VerticalAlignment="Top" Width="255" Loaded="ComboBoxSites_Loaded">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate x:DataType="model:Site">
                                            <TextBlock Text="{x:Bind Url}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Padding="3">
                                <TextBlock Text="Личность: " Padding="5" VerticalAlignment="Center"/>
                                <ComboBox HorizontalAlignment="Left" Padding="5" VerticalAlignment="Top" Width="230" Loaded="ComboBox_Loaded">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate x:DataType="model:Person">
                                            <TextBlock Text="{x:Bind name}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                        </StackPanel>

                        <GridView Grid.Row="1" x:Name="GridView" Loaded="EverydayGridView_Loaded" Width="330">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="model:PersonStats">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="150" />
                                                <ColumnDefinition Width="250" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{x:Bind PersonName}" Padding="5"/>
                                            <TextBlock Grid.Column="1" Text="{x:Bind count}" Padding="5"/>
                                        </Grid>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                        </GridView>
                    </Grid>
                </DataTemplate>
            </HubSection>
            
            <!-- Общая статистика -->
            
            <HubSection x:Name="generalSection" Header="Общая" Width="350">
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="3">
                            <TextBlock Text="Сайты: " Padding="5" VerticalAlignment="Center"/>
                            <ComboBox HorizontalAlignment="Left" Padding="5" VerticalAlignment="Top" Width="255" Loaded="ComboBoxSites_Loaded">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate x:DataType="model:Site">
                                        <TextBlock Text="{x:Bind Url}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <GridView x:Name="generalStaticGrid" Grid.Row="1" Margin="2" Loaded="GeneralStaticGrid_Loaded">
                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="model:TotalStatis">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="150" />
                                            <ColumnDefinition Width="250" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{x:Bind }" Padding="5"/>
                                        <TextBlock Grid.Column="1" Text="{x:Bind }" Padding="5"/>
                                    </Grid>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>

                    </Grid>
                </DataTemplate>
            </HubSection>
        </Hub>
        
    </Grid>
</Page>
